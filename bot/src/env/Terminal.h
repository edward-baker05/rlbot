#pragma once

// Custom terminal conditions live here. Currently empty: the env uses
// RLGymCPP's NoTouchCondition and GoalScoreCondition directly. The old
// kickoff-only FirstTouchCondition and TimeoutCondition are in git history if
// a hard episode cap is ever wanted.
